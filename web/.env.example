VITE_CLERK_PUBLISHABLE_KEY=pk_test_replace_me
# Empty means same origin (the dev and preview servers proxy /v1 to localhost:8000).
VITE_API_URL=
VITE_SENTRY_DSN=
# Tags Sentry events; the hosted builds set production or development.
VITE_SENTRY_ENVIRONMENT=

# End-to-end tests only (never VITE_-prefixed, never bundled)
CLERK_SECRET_KEY=
E2E_CLERK_USER_EMAIL=
