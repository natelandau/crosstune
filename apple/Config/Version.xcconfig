// `just bump` writes the release version here with the API's and the web client's, so the
// App Store version names the contract the app was generated from. CI sets the build number
// at archive time, since every upload needs a new one.
MARKETING_VERSION = 0.7.0
CURRENT_PROJECT_VERSION = 1
